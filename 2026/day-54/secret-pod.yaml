apiVersion: v1
kind: Pod
metadata:
  name: secret-pod
spec:
  containers:
    - name: busybox
      image: busybox:1.36
      command:
        - sh
        - -c
        - |
          echo "DB_USER=$DB_USER"
          echo "Mounted DB_USER:"
          cat /etc/db-credentials/DB_USER
          echo
          echo "Mounted DB_PASSWORD:"
          cat /etc/db-credentials/DB_PASSWORD
          sleep 3600

      env:
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_USER

      volumeMounts:
        - name: db-credentials-volume
          mountPath: /etc/db-credentials
          readOnly: true

  volumes:
    - name: db-credentials-volume
      secret:
        secretName: db-credentials
